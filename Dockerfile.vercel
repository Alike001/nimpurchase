FROM node:24-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
RUN npm run build

ENV NODE_ENV=production
EXPOSE 8787

# Vercel runs this persistent Node service behind its HTTPS proxy.
CMD ["sh", "-c", "npm run db:migrate && npm run start"]
